import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "De la Cabeza al Cielo A.C. | Derechos a la Salud y Acondroplasia",
  description: "Fundación comprometida con el acceso equitativo a tratamientos innovadores y calidad de vida para personas con acondroplasia.",
  keywords: "acondroplasia, salud, tratamientos innovadores, fundación, derechos",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
